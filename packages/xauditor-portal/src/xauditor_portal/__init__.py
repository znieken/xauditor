"""Web portal + PostgreSQL mirror for xauditor.

This package is installed optionally via `pip install xauditor-portal`. When
present alongside the main `xauditor` package it activates the report
database sink, the FastAPI backend, and the bundled Next.js frontend build.
"""

__version__ = "0.1.0"
